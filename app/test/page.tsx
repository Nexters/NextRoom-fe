"use client";

import { MakeHintModal } from "@/components/MakeHintModal";
import React from "react";

type Props = {};

const TestPage = (props: Props) => {
  return <MakeHintModal />;
};

export default TestPage;
