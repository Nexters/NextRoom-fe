import React from "react";
import MakeHintModalView from "./MakeHintModalView";

type Props = {};

const MakeHintModal = (props: Props) => {
  return <MakeHintModalView />;
};

export default MakeHintModal;
